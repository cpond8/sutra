;; macro_basic.sutra - Canonical minimal macro tests

;; From debug_minimal.sutra
(define (f) x)

;; From debug_macro.sutra
(define (test) (print "test macro"))

;; From debug_def_only.sutra (definition only, no call)
(define (greet name) (print (+ "Hello, " name "!")))

;; From test_macro_params.sutra
(define (greet name) (print name))
(greet "Alice")
(greet "Bob")