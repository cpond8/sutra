;; Parser edge cases test
;; Tests various syntax edge cases

;; Multiple expressions on same line
(print "first") (print "second")

;; Nested expressions
(print (+ 1 (+ 2 3)))

;; Mixed types in expressions
(print (list 1 "text" true))

;; Comments and whitespace
;; This is a comment
(print "after comment")

  ;; Indented comment
  (print "indented expression")

;; Empty lists
(print (list))

;; Deep nesting
(print (+ 1 (* 2 (+ 3 4))))
