;; error_cases.sutra
;; Test error handling and edge cases for atoms

;; Division by zero error
(print "Testing division by zero:")
;; This should cause an error, so we'll comment it out for now
;; (print (/ 10 0))

;; Type errors - trying to do arithmetic on non-numbers
(print "Testing type errors:")
;; (print (+ "hello" 5))

;; Arity errors - wrong number of arguments
(print "Testing arity errors:")
;; (print (+))  ;; Too few arguments
;; (print (- 10 5 2))  ;; Too many for subtraction

;; Test error atom
(print "Testing explicit error:")
;; (error "This is a test error")

;; For now, let's test valid edge cases
(print "Valid edge cases:")
(print (+ 0 0))
(print (- 0 0))
(print (* 0 5))
(print (/ 0 1))
(print (mod 5 1))
(print (eq? 0 0))
(print (not false))

;; Test with negative numbers
(print "Negative number operations:")
(print (+ -5 -3))
(print (- -10 -5))
(print (* -2 -3))
(print (/ -12 -3))
(print (mod -7 3))
